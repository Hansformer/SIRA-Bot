output-format: pylint
strictness: veryhigh

ignore-paths:
  - venv
  - __pycache__

bandit:
  run: true

dodgy:
  run: false

pylint:
  enable:
    - useless-suppression
  options:
    max-line-length: 120

pep8:
  options:
    max-line-length: 120